# MyTerm 安全設計

**繁體中文** | [English](SECURITY.en.md)

## 本機機密資料

- Google 登入狀態、同步 Master Key 與所有主機密碼保存於同一個 AES-256-GCM 本機加密保管庫。
- macOS Keychain 只保存一把隨機根金鑰；存取屬性為 `WhenUnlockedThisDeviceOnly`，不會透過備份遷移到其他裝置。
- 保管庫目錄與檔案只允許目前使用者存取，寫入採原子替換；主機資料檔不包含密碼。
- App 啟動後只解鎖根金鑰一次並保留於目前程序記憶體，帳號頁、同步、SSH 與 SFTP 共用該次解鎖結果。
- 根金鑰遺失、格式不符或保管庫驗證標籤損壞時，MyTerm 會停止存取且不覆寫既有密文。
- App 不會把已儲存密碼複製到剪貼簿，也不會記錄終端機輸出。
- 終端標籤分色只在顯示時辨識目前繪製行的短前綴，不記錄、傳送或另存終端內容；不改寫 PTY 資料、遠端指令或複製文字。標籤可能來自任何端點或普通文件，顏色不代表訊息經過驗證，也不是命令成功／安全狀態的可信判斷。可在外觀設定關閉標籤分色。
- 分頁的背景新輸出提示只在目前程序記憶體保存 Session UUID 的未讀狀態；不保存輸出內容、命令、位元組或時間，也不寫入 Logs、主機匯出、Application Support、Keychain 或跨裝置同步。App 結束後狀態即清空。
- 主機庫最近連線排序只在本機權限 `0600` 的獨立檔案保存主機 UUID 與成功時間，不保存主機名稱、位址、帳號、密碼或連線內容，也不進入主機匯出或 Firebase 同步。只有 OpenSSH 回報實際驗證成功才更新；檔案缺少或損壞時退回一般主機順序，不阻止連線。
- Logs 連線稽核屬於敏感使用軌跡，會保存互動式 SSH 連線當下的主機名稱、平台、帳號端點、來源裝置快照、開始／驗證／結束時間與結構化結果，但不接收密碼、OTP、私鑰、輸入命令、終端機輸入／輸出或 OpenSSH verbose debug。本機資料目錄與檔案權限分別為 `0700`／`0600`，採原子寫入，最多保留 30 天與 5,000 筆，且不進入主機匯出。Logs 為唯讀，沒有單筆刪除或清除全部入口；損壞檔案會隔離備份。
- 啟用同步後，只有已最終化的 Logs 紀錄會使用現有 Master Key 與 AES-256-GCM 端對端加密後寫入目前 UID 的專用 Firestore 集合。主機、帳號、位址、來源裝置、時間與結果均在密文內；連線中狀態、即時計時與 heartbeat 不上傳。固定 30 天整理會以 Firestore 回應的伺服器時間先排除本機過期資料，再移除到期密文，避免離線裝置復活舊紀錄。
- 自動同步執行診斷另存在通道隔離的 `Sync Diagnostics/events.json`，目錄／檔案為 `0700`／`0600`，最多 1 MiB／1,500 筆；載入及新增事件時移除超過 7 天的紀錄。只保存時間、隨機同步輪次、觸發與階段列舉、計數、白名單錯誤分類及數字代碼；不保存 UID、主機、帳號、IP、裝置名稱、URL、原始錯誤文字、請求內容或機密。診斷不同步、不自動上傳，只能由使用者主動複製分享；診斷寫入失敗不影響 SSH 或同步。
- 主機拖曳分類只在目前可見的「所有主機」內容區啟用，且只追蹤目前 MyTerm 視窗內、由既有主機卡片開始的滑鼠事件；主機頁被 Terminal 等功能遮住、切入分類或等待確認時會停用監聽，不接受其他 App 傳入的拖放資料，也不攔截其他畫面的拖曳。放手後必須再由使用者確認，`HostStore` 才驗證主機與目標分類並保存；取消、無效範圍及相同分類都不寫入 inventory。
- 使用密碼驗證時，只在第一次 SSH `password:` 提示自動填入已保存密碼；同一連線再次出現登入提示時不會重送舊值，而會把下一次手動輸入視為待驗證的替代候選。後續 sudo 或其他應用程式提示不會收到登入自動填入。
- 沒有本機保管庫密碼，或第一次已保存密碼遭拒絕時，MyTerm 只在 SSH 登入密碼提示後暫時追蹤輸入。必須等本機 `/usr/bin/ssh` 透過權限 `0600` 的私人診斷檔回報 `Authenticated ... using "password"`，才詢問保存或取代。再次出現登入提示時會清除失敗嘗試，再擷取下一次輸入。
- 強制改密碼流程與一般填入 gate 分離；孤立的 `New password`、OTP、`sudo` 或確認提示不會啟動擷取。只有依序辨識目前密碼、新密碼與再次確認，兩次新密碼以固定時間比較相同，且遠端輸出明確成功標記時，才詢問是否更新保管庫。MyTerm 不會靜默覆寫儲存密碼。
- 每個互動式 SSH Session 會使用權限 `0600` 的短期 OpenSSH 診斷檔判斷實際連線階段與密碼驗證結果。原始 verbose 行可能包含主機、帳號、指紋、演算法或本機私鑰路徑；UI 只接收 allow-list 的結構化摘要與有診斷價值的非 debug OpenSSH 原始錯誤，`debug1:`～`debug3:`、本機設定讀取、SSH Agent socket 與 provider 追蹤不會進入畫面，完整本機路徑也會遮蔽。診斷資料不同步、不上傳、不長期保存，暫存檔會在驗證、取消、失敗或工作階段結束後刪除。
- 使用者主動複製連線診斷時，內容包含主機端點、結構化階段、失敗分類、修正建議與同一組已清理的非 debug OpenSSH 原始錯誤；不包含 verbose debug、密碼、金鑰內容、本機私鑰完整路徑、設定檔路徑或 SSH Agent socket。
- 暫存密碼在被替換、驗證失敗、兩次輸入不一致、拒絕保存、成功保存、取消或工作階段清理時以可變緩衝區覆寫並清除。
- `keyboard-interactive` 成功時不詢問保存，因為輸入可能是 OTP 或不可重用的驗證挑戰。臨時使用者名稱也不會綁定到原主機帳號的 Keychain 項目。
- 只有工作階段帳號與主機資料中非空白的預設帳號一致時，才使用已儲存密碼；臨時切換帳號不會收到其他帳號的密碼。
- 手動「填入密碼」只把密碼位元組送入該分頁的 pseudo-terminal，隨後立即清除暫存可變緩衝區。
- 本機 Terminal 固定執行 `/bin/zsh`，只取得 SSH 共用的允許環境變數，另外加入 `SHELL=/bin/zsh`。
- 主機平台辨識會先檢查已顯示給使用者且有大小上限的終端機輸出。仍未知的平台可另外建立短時間、非互動式 SSH 連線，只讀取 `/etc/os-release`、相容的 release 檔與 `uname -a`；它不使用 `sudo`、不寫入遠端、不把密碼放進參數或一般檔案，失敗或逾時時維持通用圖示。
- Serial 只接受實際存在的 `/dev/cu.*` 或 `/dev/tty.*` 字元裝置。選項以程式參數傳給固定的 `/bin/stty` 與 `/usr/bin/screen`，不經過 Shell 字串插值。
- 版本 0.1 資料遷移保留主機 UUID，使 Keychain 關聯繼續有效，並在替換資料格式前建立僅擁有者可讀的備份。
- 更名為 MyTerm 時刻意保留原 Bundle ID、Keychain service 與 Application Support 目錄，使既有密碼仍對應相同 UUID。
- 刪除密碼時會移除保管庫中的精確主機項目，並為尚未清除的舊版 Keychain 項目保存不含機密的抑制標記，避免舊密碼復活。舊項目權限異常不會阻止刪除主機資料。
- schema 3 多階層群組遷移會先建立權限 `0600` 的備份；遺失父群組或循環關係會修復到根層級，不刪除主機或群組。
- 只有使用者按下「載入」或「同步」時才讀取 `~/.ssh/known_hosts`。匯入的信任快照與顯示索引保存在獨立的僅擁有者可讀檔案，不安裝監視器或背景同步。
- MyTerm App 不讀取、解密或擷取 Termius Vault 資料庫。Repository 的選用轉換工具只接受相容的主機／群組中繼資料輸入，在使用者自己的 Mac 上透過已安裝的 Termius 執行環境轉換欄位；它沒有密碼、Passphrase、Token 或私鑰欄位的讀取與輸出程式碼。Termius 內部格式可能隨版本改變，完整限制與操作方式見 [Termius 主機資料遷移](TERMIUS_MIGRATION.md)。
- MyTerm 與 Termius 主機資料匯入限制檔案大小與筆數，驗證群組階層及欄位、預覽衝突、配置新的主機 UUID，並在套用前建立權限 `0600` 的 Inventory 備份。
- `Exports/` 內的遷移輸出即使不含憑證，仍可能揭露主機名稱、IP、帳號及內部群組結構；整個目錄由 Git 排除，輸出工具以 `0600` 建立檔案，發布安全檢查也會拒絕未受排除規則保護的私人輸出。
- 自訂匯入會先過濾勾選項目再分析衝突。只建立實際接受主機所需的上層群組；跳過或無效資料不留下空群組。
- MyTerm 匯出採明確允許欄位的格式，不包含 Keychain 密碼、私鑰內容、passphrase、Token 或本機私鑰路徑。匯出的 JSON 仍是明文，可能包含主機位址、帳號與備註，使用者必須自行保管。

## SSH 信任

- 連線使用系統 `/usr/bin/ssh`，不自行實作加密協定。
- 主機金鑰保存在 App 專用、權限 `0600` 的 `known_hosts`。
- `StrictHostKeyChecking=ask` 要求確認新主機，並阻擋已改變的金鑰。
- 系統預設模式沿用目前 OpenSSH 的演算法政策。
- RSA 相容與自訂演算法只以單一主機連線的程序參數生效，不修改 `~/.ssh/config` 或全域政策。

## 目前已知限制

- 自動填入只適用於設定為密碼驗證的工作階段第一次密碼提示。惡意端點若已通過主機金鑰驗證，仍位於該次連線的憑證信任範圍內。
- 手動填入無法證明提示來源；只有在確定連到正確主機且畫面確實是密碼提示時才使用。
- App 只把私鑰路徑當作主機設定保存，不會複製私鑰內容。
- Logs 目前只涵蓋由主機庫建立的互動式 SSH；本機 Terminal、SFTP、Serial 與功能啟用前的歷史不會產生或補建稽核紀錄。
- 正式版本目前未使用 Apple Developer ID 與公證，第一次手動下載可能需要使用者在 macOS「隱私權與安全性」允許開啟。零費用自簽憑證沒有 Apple Team ID，Keychain 可能把不同建置視為不同程式身分；1.0.1 以單一根金鑰把更新後的驗證數量限制為整個 App 一次，而不是每個機密或每台主機各一次。App 內更新仍使用獨立的 Sparkle Ed25519 簽章驗證。
- App 內更新需要可替換的安裝位置；若從 ZIP、唯讀映像、下載後的 App Translocation 或其他暫時位置執行，Sparkle 會安全停止。正式使用應將 `MyTerm.app` 放進「應用程式」資料夾。
- `sudo`／`su` 密碼不會自動送出；MyTerm 只在辨識到安全密碼提示時開放按鈕或快捷鍵一鍵填入，讓使用者保留最後確認權。
- 平台辨識採保守的 best-effort；畫面沒有明確作業系統或網路設備標記時會維持通用圖示。
- Serial 可能因轉接器中斷、被其他程式占用或 macOS／驅動權限而失敗。目前尚無實體 USB Serial 裝置完成全流程硬體驗證。
- 跨裝置同步為預設關閉的選用功能。啟用後，主機、群組、密碼與已結束 Logs 只會以逐筆端對端加密密文傳送至 Firebase；Master Key 與解密後密碼只保存在各台 Mac 的本機加密保管庫，保管庫根金鑰為 ThisDeviceOnly Keychain 項目。私鑰檔案、私鑰路徑與 known_hosts 不同步；架構與資料流見 [系統架構](ARCHITECTURE.md)。自行建置同步後端時必須部署 repository 內的 Security Rules，不得以測試用全開規則代替；完整前置作業見 [Firebase 自架同步設定](FIREBASE_SETUP.md)。
- 正式、development 與 update-lab 的 Google refresh token 位於各自通道的本機加密保管庫。只有 production 可從早期正式 Keychain 項目做一次相容遷移；Dev／Update Lab 不讀取正式舊 session，並會一次性清理過去可能誤匯入其自身保管庫的 token，不會刪除正式版 session。
- 主機與群組刪除會同步端對端驗證的加密 tombstone。套用遠端刪除前會備份本機清單；遠端主機刪除後再清理該台 Mac 保管庫中的密碼，舊 Keychain 項目權限異常不會阻止清單刪除。

## SFTP 資源與更新驗證限制

- SFTP READ 拒絕沒有資料的成功狀態，以及超過此次要求長度的回應。目錄操作在累計 100,000 筆、64 MiB 回應或 10,000 次回應時停止；遞迴深度上限為 64。超限回報錯誤，不把截斷清單當作完整結果。
- 初始化總期限為 30 秒，每次協定回應的期限為 30 秒；取消可停止初始化與阻塞 I/O，只終止該 transport 自建的子程序。各次請求正常完成時，大檔可持續傳輸。
- 部署前以可信公鑰驗證 feed、ZIP 與更新說明簽章，再解壓資產；已簽署 feed 綁定來源 commit 與執行時相依清單，checksum 不能單獨證明來源。見 [安全維護指南](SECURITY_MAINTENANCE.md)。
